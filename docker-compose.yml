services:
  video-compressor:
    build: .
    ports:
      - "3000:3000"
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - "${RENDER_GID}"
    environment:
      TZ: "${TZ:-UTC}"
      TRASH_ROOT: /trash
    volumes:
      - "${MEDIA_DIR}:/media"
      - "${TRASH_DIR}:/trash"
      - queue-data:/data
    restart: unless-stopped

volumes:
  queue-data:
